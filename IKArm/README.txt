README

compiled using Visual Studio on Windows

use numpad 1, 2, 3, 5 to move points along x, y axis, 4, 6 to move along y axis

pressing 7 causes arm to follow circular path based on its current location (if possible)

Arm uses process of cyclic coordinate descent to reach the movable point onscreen